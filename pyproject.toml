[project]
name = "openharness"
version = "0.1.0"
description = "Open Source Harness CAD"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "PySide6>=6.8",
]

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]

